enum BorderStyle {
    case solid
    case dashed
    case dotted
    case double
}

enum OutputMode {
    case pixel
    case point
}

enum OutputFormat {
    case text
    case json
}

enum ResizeHandle {
    case none
    case topLeft, topRight, bottomLeft, bottomRight
}
